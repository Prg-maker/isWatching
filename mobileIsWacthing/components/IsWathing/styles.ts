import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
  container:{
    height: '100%',
    justifyContent:'flex-end',
    alignItems:'flex-end',

  },

  content:{
    flexDirection: 'row',
    alignItems:'center',
    marginBottom: 20,
    marginRight: 20
  }
})  